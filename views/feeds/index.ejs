<% include ../partials/header %> 

 <div class="container">
     <header class="jumbotron">
         <div class="container">
             <h1>Welcome To Slambook</h1>
             <p>
                <a class="btn btn-primary btn-large" href="/feeds/new">Add NewFeed</a>
             </p>
         </div>
     </header>
     
     <div class="row text-center" style="display:flex; flex-wrap: wrap;">
        <% feeds.forEach(function(feed){  if(feed.date <= new Date()){%>
            <div class="col-md-3 col-sm-6">
                <div class="thumbnail">
                   <img src="<%= feed.image %>">
                   <div class="caption">
                       <h4><%= feed.name %></h4>
                   </div>
                   <p>
                       <a href="/feeds/<%= feed._id %>" class="btn btn-primary">More Info</a>
                   </p>
                </div>
            </div>
        <% }}); %>
    </div>
</div>

<% include ../partials/footer %>
